# Widget Manual

A small document to feed the pipeline.

## Overview

The pipeline turns this Markdown into HTML with pandoc, then stamps the
footer with version information taken from git at build time.

## Details

Nothing here depends on the content; edit this file and rebuild to watch
only the affected steps re-run.
