name: CI

permissions:
  contents: read

on:
  push:
    branches: [main]
    tags:
      - 'v*'  # Also run CI on version tags
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: "latest"
      - name: Install dependencies
        run: uv sync
      - name: Run ruff
        run: uv run ruff check .
      - name: Run ruff format check
        run: uv run ruff format --check .
      - name: Run ty typecheck
        run: uvx ty check pcons/ examples/

  docs:
    # Both docs builds are seconds each; a broken one otherwise surfaces
    # only when someone reads readthedocs or runs `make docs`.
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: "latest"
      - name: Install pandoc and ninja
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            -o Acquire::Retries=5 -o Acquire::http::Timeout=20 \
            pandoc ninja-build
      - name: Build the dogfood demo page (pcons building its own docs)
        run: uv run pcons -C docs
      - name: Build the site (what ReadTheDocs publishes)
        run: uv run --extra docs mkdocs build --strict

  test:
    needs: lint
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, ubuntu-22.04, macos-latest, windows-latest]
        python-version: ["3.11", "3.12", "3.13", "3.14"]
        exclude:
          # Only test oldest Python on Ubuntu 22.04
          - os: ubuntu-22.04
            python-version: "3.12"
          - os: ubuntu-22.04
            python-version: "3.13"
          - os: ubuntu-22.04
            python-version: "3.14"
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: "latest"
      - name: Set up MSVC (Windows only)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@0b201ec74fa43914dc39ae48a89fd1d8cb592756 # v1
      - name: Set Python version
        env:
          PYTHON_VERSION: ${{ matrix.python-version }}
        run: uv python install $PYTHON_VERSION
      - name: Install LaTeX (one Ubuntu job only)
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.14'
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          # Best-effort install: examples/32_latex skips when latexmk is
          # absent, so a flaky/slow apt mirror degrades to "LaTeX example
          # skipped" instead of hanging the job and blocking releases.
          # Retries + a short per-connection timeout keep a degraded mirror
          # from burning the whole job budget (it was stalling ~60s/package).
          sudo apt-get update -qq || true
          sudo apt-get install -y --no-install-recommends \
            -o Acquire::Retries=5 -o Acquire::http::Timeout=20 \
            texlive-latex-base texlive-bibtex-extra latexmk \
            || echo "::warning::LaTeX install failed (apt mirror?); LaTeX example tests will be skipped"
      - name: Install dependencies
        run: uv sync
      - name: Extend Conan settings for new Apple Clang versions (macOS)
        if: runner.os == 'macOS'
        run: |
          CONAN_HOME=$(uvx conan config home)
          # Conan rejects a compiler.version its settings.yml does not list, and
          # the runner image moves faster than Conan releases -- it reached
          # apple-clang 21 while this said 18. Listed with headroom so the next
          # Xcode bump does not fail the build.
          cat > "$CONAN_HOME/settings_user.yml" << 'EOF'
          compiler:
              apple-clang:
                  version: ["17", "18", "19", "20", "21", "22", "23", "24"]
          EOF
      - name: Run unit tests (Windows)
        if: runner.os == 'Windows'
        run: uv run pytest -n auto --cov=pcons --cov-report=xml --ignore=tests/test_examples.py -k "not 21_msvcup_hello"
      - name: Run tests (non-Windows)
        if: runner.os != 'Windows'
        # Swift examples run in the dedicated test-swift job: swiftc's
        # cold-start (implicit Glibc/Foundation module builds on Linux) is
        # slow under xdist CPU contention and was flaking the cli-variant
        # timeout here.
        run: uv run pytest -n auto --cov=pcons --cov-report=xml -k "not 21_msvcup_hello and not 46_swift and not 47_swift and not 48_swift and not 49_swift"
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          files: coverage.xml
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: ${{ matrix.os }}

  test-examples-windows:
    name: "test examples (windows-latest, ${{ matrix.python-version }})"
    needs: lint
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13", "3.14"]
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: "latest"
      - name: Set up MSVC
        uses: ilammy/msvc-dev-cmd@0b201ec74fa43914dc39ae48a89fd1d8cb592756 # v1
      - name: Set Python version
        env:
          PYTHON_VERSION: ${{ matrix.python-version }}
        run: uv python install $PYTHON_VERSION
      - name: Install dependencies
        run: uv sync
      - name: Run example tests
        run: uv run pytest tests/test_examples.py -n auto --cov=pcons --cov-report=xml -k "not 21_msvcup_hello"
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          files: coverage.xml
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: windows-examples

  test-swift:
    name: "test swift examples"
    needs: lint
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: "latest"
      - name: Install dependencies
        run: uv sync
      - name: Verify Swift toolchain
        id: verify-swift
        continue-on-error: true
        run: swiftc --version
      - name: Run Swift example tests
        if: steps.verify-swift.outcome == 'success'
        run: uv run pytest tests/test_examples.py --cov=pcons --cov-report=xml -v -k "46_swift or 47_swift or 48_swift or 49_swift"
      - name: Warn if Swift missing from runner image
        if: steps.verify-swift.outcome != 'success'
        run: echo "::warning::swiftc not found on ${{ matrix.os }} runner image; Swift example tests skipped"
      - name: Upload coverage to Codecov
        if: steps.verify-swift.outcome == 'success'
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          files: coverage.xml
          token: ${{ secrets.CODECOV_TOKEN }}

  test-qt:
    name: "test qt examples"
    needs: lint
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: "latest"
      - name: Set up MSVC (Windows only)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@0b201ec74fa43914dc39ae48a89fd1d8cb592756 # v1
      - name: Install Qt (Linux)
        if: runner.os == 'Linux'
        # base: moc/uic/rcc + qtpaths6; declarative: Qml module +
        # qmltyperegistrar; qml6-module-qtqml: runtime for `import QtQml`;
        # l10n/tools: lrelease/lupdate (package split varies by release,
        # so install both — example 55 skips gracefully if absent).
        run: |
          sudo apt-get update
          sudo apt-get install -y qt6-base-dev qt6-base-dev-tools \
            qt6-declarative-dev qt6-declarative-dev-tools \
            qml6-module-qtqml qml6-module-qtqml-models \
            qml6-module-qtqml-workerscript \
            qt6-tools-dev-tools qt6-l10n-tools
      - name: Install Qt (macOS)
        if: runner.os == 'macOS'
        run: brew install qt
      - name: Install Qt (Windows)
        if: runner.os == 'Windows'
        # aqtinstall fetches the official-installer layout (no pkg-config
        # files) — exercising the qtpaths discovery route in CI.
        run: uvx --from aqtinstall aqt install-qt windows desktop 6.8.3 win64_msvc2022_64 --outputdir C:\Qt
      - name: Install dependencies
        run: uv sync
      - name: Verify Qt is discoverable
        # Guard against a green job in which every example silently
        # skipped because the Qt install step didn't produce a usable Qt.
        run: >
          uv run python -c "from pcons.toolchains.qt.toolchain import
          _find_tool, _locate_tool_dirs;
          moc = _find_tool('moc', _locate_tool_dirs());
          print('moc:', moc);
          raise SystemExit(0 if moc else 'Qt not discoverable')"
      - name: Run Qt example tests
        run: uv run pytest tests/test_examples.py --cov=pcons --cov-report=xml -v -k "52_qt or 53_qt or 54_qt or 55_qt or 56_qt"
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          files: coverage.xml
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: qt-examples

  test-wasm:
    name: "test wasm (emscripten + wasi)"
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: "latest"
      - name: Install Emscripten
        uses: mymindstorm/setup-emsdk@4528d102f7230f0e7b276855c01ea1159be0e984 # v16
        with:
          actions-cache-folder: 'emsdk-cache'
      - name: Install wasi-sdk
        run: |
          WASI_VERSION=32  # v31 was broken on macOS: hardcoded rpath (wasi-sdk#622), fixed in v32
          WASI_VERSION_FULL=${WASI_VERSION}.0
          ARCH=$(uname -m)
          case "$(uname -s)" in
            Linux)  OS=linux ;;
            Darwin) OS=macos ;;
            *)      echo "Unsupported OS"; exit 1 ;;
          esac
          curl -sSfL "https://github.com/WebAssembly/wasi-sdk/releases/download/wasi-sdk-${WASI_VERSION}/wasi-sdk-${WASI_VERSION_FULL}-${ARCH}-${OS}.tar.gz" | tar xz
          sudo mv "wasi-sdk-${WASI_VERSION_FULL}-${ARCH}-${OS}" /opt/wasi-sdk
      - name: Install wasmtime
        uses: jcbhmr/setup-wasmtime@960c367a99921eb0b02f5778fce9ae8f110bf0f0 # v2
      - name: Install dependencies
        run: uv sync
      - name: Verify Emscripten tools
        run: |
          emcc --version
          node --version
      - name: Verify WASI tools
        id: verify-wasi
        continue-on-error: true
        run: |
          wasmtime --version
          /opt/wasi-sdk/bin/clang --version
      - name: Run Emscripten example tests
        run: uv run pytest tests/test_examples.py --cov=pcons --cov-report=xml -v -k "23_wasm_emscripten"
      - name: Run WASI example tests
        if: steps.verify-wasi.outcome == 'success'
        env:
          WASI_SDK_PATH: /opt/wasi-sdk
        run: uv run pytest tests/test_examples.py --cov=pcons --cov-report=xml --cov-append -v -k "22_wasm_wasi"
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          files: coverage.xml
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: wasm

  test-bare-metal:
    name: "test bare-metal example (arm-none-eabi + qemu)"
    needs: lint
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          version: "latest"
      - name: Install the ARM bare-metal toolchain and QEMU
        env:
          DEBIAN_FRONTEND: noninteractive
        # No "|| true" here, unlike the best-effort LaTeX install in the test
        # job: example 68 skips itself when arm-none-eabi-gcc is absent, so a
        # tolerated install failure would turn this job green while testing
        # nothing.
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            -o Acquire::Retries=5 -o Acquire::http::Timeout=20 \
            gcc-arm-none-eabi binutils-arm-none-eabi qemu-system-arm
          arm-none-eabi-gcc --version | head -1
          qemu-system-arm --version | head -1
      - name: Install dependencies
        run: uv sync
      - name: Run the bare-metal example
        # The example skips itself when a tool is missing, so a green job is
        # not on its own evidence that it ran: check that it did.
        run: |
          set -o pipefail
          uv run pytest tests/test_examples.py --cov=pcons --cov-report=xml -v \
            -k "69_bare_metal" | tee result.txt
          if grep -q '69_bare_metal-ninja] SKIPPED' result.txt; then
            echo "::error::the bare-metal example skipped instead of running"
            exit 1
          fi
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          files: coverage.xml
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: bare-metal

  test-msvcup:
    name: "test msvcup (windows, no VS)"
    needs: lint
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: "latest"
      # No MSVC setup - msvcup installs its own toolchain
      - name: Set Python version
        run: uv python install 3.14
      - name: Install dependencies
        run: uv sync
      - name: Run msvcup example tests
        run: uv run pytest tests/test_examples.py --cov=pcons --cov-report=xml -k "21_msvcup_hello" tests/contrib/test_msvcup.py -v
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          files: coverage.xml
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: msvcup

  test-cxx-modules:
    name: "test C++20 modules (${{ matrix.os }})"
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: "latest"
      - name: Set up clang tools (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo update-alternatives --install /usr/bin/clang-scan-deps clang-scan-deps \
            /usr/bin/clang-scan-deps-18 100
          sudo update-alternatives --install /usr/bin/clang++ clang++ \
            /usr/bin/clang++-18 100
          sudo update-alternatives --install /usr/bin/clang clang \
            /usr/bin/clang-18 100
      - name: Add Homebrew LLVM to PATH (macOS)
        if: runner.os == 'macOS'
        run: echo "$(brew --prefix llvm@18)/bin" >> $GITHUB_PATH
      - name: Install dependencies
        run: uv python install 3.12 && uv sync
      - name: Install libc++ for `import std;` (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get install -y libc++-18-dev libc++abi-18-dev
      - name: Install gcc-15 (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y software-properties-common
          
          sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
          sudo apt-get update

          sudo apt-get install -y gcc-15 g++-15

          sudo update-alternatives --install /usr/bin/gcc gcc \
            /usr/bin/gcc-15 100
          sudo update-alternatives --install /usr/bin/g++ g++ \
            /usr/bin/g++-15 100
      - name: Run C++20 modules example tests
        run: uv run pytest tests/test_examples.py --cov=pcons --cov-report=xml -k "29_cxx_modules or 30_cxx_partitions or 31_cxx_modules_optin or 32_cxx_import_std or 35_cxx_modules_deps or 39_bmi_compat" -v
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          files: coverage.xml
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: cxx-modules

  test-rez:
    name: "test rez integration (${{ matrix.os }})"
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        # rez targets Linux/macOS; the example skips on Windows (see its test.toml).
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: "latest"
      - name: Install dependencies (with rez)
        # rez is installed into the project venv so its plugin lands on the
        # same sys.path pcons does (that's how rez discovers the build_system
        # plugin). uv sync would uninstall it, so every run below uses
        # --no-sync.
        run: |
          uv python install 3.12
          uv sync
          uv pip install rez
      - name: Run rez integration unit tests
        run: uv run --no-sync pytest tests/integration/ --cov=pcons --cov-report=xml -v
      - name: Type-check the rez integration (with rez installed)
        # The default `ty` lint excludes pcons/integrations/rez because rez
        # isn't in that environment; here rez is importable, so type-check it
        # against the real rez API. Explicit file paths bypass the exclude.
        run: find pcons/integrations/rez -name '*.py' -print0 | xargs -0 uvx ty check
      - name: Build the rez test packages and run the example end-to-end
        # hello_lib (rez's built-in cmake build_system) then hello_app (pcons's
        # build_system plugin — exercises PconsBuildSystem.build()); then run
        # example 45's test inside the resolve so REZ_USED_RESOLVE is set.
        run: |
          export REZ_LOCAL_PACKAGES_PATH="$RUNNER_TEMP/rez_packages"
          export REZ_RELEASE_PACKAGES_PATH="$RUNNER_TEMP/rez_packages"
          export REZ_PACKAGES_PATH="$RUNNER_TEMP/rez_packages"
          mkdir -p "$REZ_LOCAL_PACKAGES_PATH"
          VENV="$PWD/.venv"
          export PATH="$VENV/bin:$PATH"
          # rez builds a clean resolved environment that keeps only system
          # paths; on macOS that drops Homebrew's bin, so ninja (needed by the
          # pcons build_system plugin and the example build) goes missing.
          # Inherit the parent PATH into resolved environments.
          printf 'parent_variables = ["PATH"]\n' > "$RUNNER_TEMP/rezconfig.py"
          export REZ_CONFIG_FILE="$RUNNER_TEMP/rezconfig.py"
          ( cd examples/45_rez_integration/rez_packages/hello_lib && rez-build -i )
          ( cd examples/45_rez_integration/rez_packages/hello_app && rez-build -i )
          # Use the venv python by absolute path: rez-env's resolved
          # environment does not carry uv on PATH.
          rez-env hello_lib -- "$VENV/bin/python" -m pytest tests/test_examples.py \
            --cov=pcons --cov-report=xml --cov-append -k "45_rez_integration" -v
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          files: coverage.xml
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: rez

  release:
    name: Create Release & Publish
    needs: [test, test-examples-windows, test-wasm, test-msvcup, test-cxx-modules, test-rez]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: "latest"
      - name: Build package
        run: uv build
      - name: Sign distributions with Sigstore
        uses: sigstore/gh-action-sigstore-python@790bc6befb9d733738f18d8f895854b453640ec9 # v3.5.0
        with:
          inputs: ./dist/*.tar.gz ./dist/*.whl
          upload-signing-artifacts: false
          release-signing-artifacts: false
      - name: Generate Changelog
        run: .github/release_message.sh > release_message.md
      - name: Create GitHub Release
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          body_path: release_message.md
          files: |
            dist/*.tar.gz
            dist/*.whl
            dist/*.sigstore.json
      - name: Publish to PyPI
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
        run: uvx twine upload dist/*.tar.gz dist/*.whl
